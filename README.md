# PL-SQL-Stored-Procedure-to-Update-Attributes
This query demonstrates a PL/SQL stored procedure that updates an employee’s salary using input parameters. It includes Oracle concepts such as CREATE OR REPLACE PROCEDURE, IN parameters, UPDATE statements, transaction control with COMMIT and ROLLBACK, and basic exception handling.
